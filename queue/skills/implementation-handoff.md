# Implementation Handoff

Use this skill after completing a queue item.

Include:
- Queue ID
- Branch name
- PR URL
- Commands run
- Acceptance criteria evidence
- Operator steps:
  `npm run queue:archive-top` or `npm run queue:archive -- <QUEUE_ID>`
  `npm run queue:validate`
  `npm run queue:pr-merge`
