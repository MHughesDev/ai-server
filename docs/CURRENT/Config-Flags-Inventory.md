# Config and feature-flag inventory

**Purpose:** Env vars and flags from `src/config/schema.ts` with defaults and “enforced vs advisory” notes.

**Status:** TBD — populate during documentation finalization (see `docs/TARGET/Documentation-Finalization-Plan.md` Phase 2).
