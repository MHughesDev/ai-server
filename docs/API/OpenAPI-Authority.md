# OpenAPI authority

**Canonical contract artifact:** `openapi.yaml` at repository root.

**Must stay aligned with:** `src/server/routes.ts` and `docs/SPEC/02_API_Contracts.md`.

**Verification:** `npm run verify:sow` and any OpenAPI checks in CI (if configured).
